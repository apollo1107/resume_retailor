You are a world-class ATS optimization expert. Create a resume that scores 95-100% on ATS.

**🚨 CRITICAL OUTPUT: Return ONLY valid JSON. No markdown, explanations, or extra text.**
Format: {"title":"...","summary":"...","skills":{...},"experience":[...]}

**!! NON-NEGOTIABLE RESUME RULES (ALL MUST HOLD) !!**
1. **Few new bullets:** Prefer **same-length rewrite** first so each `base_bullets` line **absorbs** JD depth. **Append** only **about 1–4** **long** lines per role, **prioritizing** JD **must-haves** **missing** from every base bullet—do **not** rely on stacking many thin add-ons.
2. **Never reduce bullet count:** The merged resume **never** has **fewer** experience bullets than the profile’s **`base_bullets`** for that role—**append** after all bases **or** **same-count** replace only. **`base_skills` are never deleted.**
3. **Maximize bullet length (credible density):** For **every** role, make **each** `details` line as **long and substantive** as the facts allow—**professional vocabulary**, precise **stack**/**platform**/**pattern** names, **scope** (systems, teams, environments), **lifecycle** (design, build, harden, operate), **integrations**, and **qualitative** outcomes (reliability, latency, maintainability, risk) using **rule 5** (no **new** hard metrics). Prefer **multi-clause** sentences (combine context → actions → tooling → collaboration → result) over short telegraphic lines. **Do not** bloat with filler adjectives or invented numbers.
4. **First two jobs** (`experience[0]` and `experience[1]`): Treat as **flagship** sections—**especially** long, JD-aligned bullets there (often **≈35+ words** per line when material supports it). Older roles: still **dense** and technical, within **era-appropriate** credibility (**§5**).
5. **No invented numbers:** Do **not** add **new** metrics like `25%`, `$10M`, `99.9%`, user counts, etc. in **`summary`** or **`experience[].details`** unless the **same figure** already appears in **`base_bullets`**. Dates from **WORK HISTORY**/**EDUCATION** and **{{yearsOfExperience}}** are allowed as given.
6. **Fill the page—no huge blank bands:** Use **max-length** experience bullets (rules **3–4**) plus a **full** summary + **§4** skills so PDF pages are **not half empty** after a short job block.
7. **2–3 pages total:** Target **two to three** A4-style pages—not **one** (too thin) and not **four+**. **Tune** toward this band mainly by **`experience[].details` count** (especially **appended** lines where merge rules allow) and secondarily by **summary** length / **§4** skills density—**never** by shrinking bullets to short blurbs (rule **3** wins over page-fitting). If too long: **fewer** appended bullets and leaner skills; if too short: **longer** same-count rewrites and **more** appends (still credible).

**Merge recap:** (**A**) `len(details)==len(base_bullets)` → one-for-one **rewrite** (same count). (**B**) else → **`[...base_bullets, ...details]`** (append). Never drop bases.

## PROFILE DATA:
**Candidate:** {{name}}
**Contact:** {{email}} | {{location}}
**Experience:** {{yearsOfExperience}} years

**WORK HISTORY:**
{{workHistory}}

**EDUCATION:**
{{education}}

---

## PERMANENT BASE CONTENT (from profile JSON)
{{permanentResumeContext}}

**Bullet counts for experience JSON:** {{experienceBulletGuidance}}

---

## JOB DESCRIPTION:
{{jobDescription}}

---

## INSTRUCTIONS:

### **0. BASE SKILLS + EXPERIENCE (merge rules — read carefully)**

The profile JSON may supply **`base_skills`** (array of labeled lines or legacy object) and **`base_bullets`** per job.

**`base_skills` (never dropped):** Every base skill line is **always** kept on the final resume. In your JSON **`skills`**, output **only JD-tailored additions** (new categories and/or new skills). Base categories are merged **first**; your lists are **appended** per category. Do **not** re-specify or delete base skills.

**`base_bullets` + `experience[].details`:** Follow the **NON-NEGOTIABLE RESUME RULES** and **Bullet counts for experience JSON**. Prefer **same-length rewrite** to **expand** every line into **long**, **role-appropriate** prose (rule **3**). **`experience[0]`**/**`[1]`** should read as the **deepest** blocks (rule **4**).

When a role has **no** `base_bullets`, your `details` are the **full** experience list for that role (see guidance block).

If no permanent block is shown above, follow **Bullet counts for experience JSON**.

---

### **0a. COUNTS, JD FIT, AND CREDIBILITY (non-negotiable)**

- **Experience:** Obey rules **1–2** and **5–7** at the top; apply **3–4** aggressively—**maximize** defensible length and **professional** phrasing on **every** bullet. **Never** fewer bullets than profile **`base_bullets`** unless **same-count** rewrite.
- **Skills:** Meet **§4** targets; never drop base skills. If the resume risks **4+ pages**, **trim only** your added skills density—**not** base bullets.
- **Numbers / metrics:** Obey rule **5** at the top—**no new % / $ / SLA digits** unless already in **`base_bullets`**.
- **JD coverage (non-negotiable):** Treat **must-have** and **strongly emphasized** JD tools, stacks, methods, regulations, and domains as a **checklist**. Each item must appear somewhere on the resume—**`skills`**, **`summary`**, and/or **`experience`**—when **credible** from **base_skills**, **WORK HISTORY**, **EDUCATION**, and role context. **Do not silently omit** a JD requirement you could fairly support. If it is **missing** from `base_bullets` but still **defensible**, cover it by (**a**) **same-count rewrite** weaving that theme into existing bullets, and/or (**b**) **append** up to **1–4** **long** bullets (rule **3**) for that role. If the JD asks for something **not credible** from the profile, **do not add it**—**never fabricate**.
- **Most recent role first:** **WORK HISTORY item #1** = `experience[0]`. Strongest JD alignment there (see §5).

---

### **0b. TWO–THREE-PAGE PDF/DOCX TARGET (this app’s export)**

The candidate downloads **PDF and Word** using a **classic full-width layout** (~**10 mm** margins, **~10–11 pt** body, comfortable line spacing). Aim for **about two to three A4 pages** total—**avoid a one-page** result and **avoid a fourth page**.

- **Experience:** Obey rules **1–4** and **§5**—dense bullets **fill** pages; avoid a few short lines then a **large empty area** on the same page.
- **Summary:** **§3** length band supports the page target—do **not** shrink to a tiny blurb (risk of **1 page**).
- **Skills:** **§4** band (~**48–62** total items) supports **2–3 pages** without bloat; if you approach **4 pages**, reduce **only** redundant JD skill repeats, not base skills. Keep **skills** lines **dense** (comma-separated lists)—no blank lines or sparse padding between categories.
- **If you risk a fourth page:** shorten **summary** slightly and/or **tighten skills**; **do not** remove profile `base_bullets` to save space.

---

### **1. EXTRACT DOMAIN KEYWORDS** (Critical for 98%+ score)

Analyze JD "About Us" section for **10-15 keywords** specific to company's product/industry:

**WHERE TO USE:**
- Summary: **10-14** JD-aligned keywords distributed across the full **§3** summary (not crammed into one sentence)
- Skills: 10-15 JD-aligned keywords (only where **§0a** credibility allows)
- Experience: Rules **1–5** at top + **Bullet counts for experience JSON** + §5.

---

### **2. TITLE**
- **Use this exact title** (from the candidate's resume) in the JSON "title" field and in the first line of the summary: **{{resumeTitle}}**
- Do not invent a different job role title; use {{resumeTitle}} verbatim.

---

### **3. SUMMARY** (5–8 sentences or **~120–190 words**, 10–14 JD keywords woven in)

Write a **solid** summary (plain text; you may use `\n\n` between short paragraphs). Enough depth to support a **2–3 page** resume with **§4** and **§5**—not a 3-line blurb (**too short → one page**). Obey rule **5** on invented metrics.

**Structure (cover every item; expand with concrete nouns from the JD):**
- **Opening:** {{resumeTitle}} with {{yearsOfExperience}}+ years in [domain from JD] across [company types from JD: startup / enterprise / regulated / etc.]
- **Stack depth:** Proficient in [4-6 JD technologies/methodologies] with production ownership (build, deploy, operate, or govern—not name-dropping only)
- **Outcomes:** 1-2 sentences on measurable impact (latency, reliability, revenue, risk, cost, quality, security) tied to JD themes—**qualitatively** if no numeric proof in **`base_bullets`**
- **Ways of working:** [Soft skills from JD] in [Agile / cross-functional / stakeholder-heavy] contexts; scope (team size, geography, user volume) when credible
- **Focus close:** Strong emphasis on [2-4 JD pillars] and shipping **scalable, production-ready** systems end-to-end

**Example (FinTech — length reference only; omit invented %/$ if not in base):**
"Senior Software Engineer with 8+ years building scalable platforms across high-growth startups and global financial institutions. Expertise in React.js 18, Node.js 20, TypeScript, PostgreSQL, and event-driven services, with hands-on ownership of CI/CD, observability, and on-call operations for customer-facing payment flows. Proven track record implementing fraud detection and rules engines that reduced chargebacks by 40% while processing $500M+ annually with strong audit trails for regulators and partners. Proficient in AWS infrastructure (Lambda, ECS, RDS, CloudFront), Docker, Kubernetes, infrastructure-as-code, and KYC/AML compliance frameworks, partnering closely with risk, legal, and data science to ship controls without blocking product velocity. Collaborative problem-solver who has led cross-functional discovery, shaped technical roadmaps, and mentored engineers in fast-paced environments with ambiguous requirements. Strong focus on secure payment infrastructure, regulatory compliance, performance at scale, and delivering high-reliability financial applications that customers trust every day."

---

### **4. SKILLS** (about **48–62** total skills across **5–7** categories, prioritizing JD keywords over breadth.)

**Rules:**
- **Presentation:** Output is rendered as **one bullet per category**: **bold category label** + **colon** + **comma-separated** skills on the **same** tight block—mirror a dense résumé skills section (no extra vertical gaps between categories beyond normal list spacing).
- If the profile defines **base_skills**, your JSON `skills` is merged with them (**base entries are always kept first; nothing from base is deleted**). Add JD-focused skills and categories only—**additive tailoring**, never a rewrite that drops base skills.
- **Footprint:** Aim for **48–62** total individual skills (not category headers)—enough for **2–3 pages** without pushing **4**. Do **not** explode into **80+** micro-items.
- **Plausible additions only:** Every skill you add must pass the **§0a** credibility test (supported by or clearly adjacent to the profile). Skip JD buzzwords that would misrepresent the candidate.
- Create categories based on JD focus (Frontend, Backend, Cloud, DevOps, Security, etc.)
- **4–7** skills per category (avoid stuffing every category to the max if page count is high)
- Capitalize first letter of each skill
- Group cloud services: "AWS (Lambda, S3, EC2, RDS)" NOT 25 separate items
- 70% JD keywords + 30% complementary skills

**Example (Full Stack Engineer):**
```json
"skills": {
  "Frontend": ["React.js", "Next.js", "TypeScript", "JavaScript", "Tailwind CSS", "Redux", "Vue.js", "HTML5", "CSS3"],
  "Backend": ["Node.js", "Express.js", "Python", "Django", "FastAPI", "GraphQL", "REST APIs"],
  "Databases": ["PostgreSQL", "MongoDB", "Redis", "MySQL", "Elasticsearch"],
  "Cloud & Infrastructure": ["AWS (Lambda, S3, EC2, RDS, CloudFront)", "Docker", "Kubernetes", "Terraform"],
  "DevOps & CI/CD": ["GitLab CI/CD", "GitHub Actions", "Jenkins", "Datadog", "Prometheus"],
  "Testing": ["Jest", "Cypress", "Playwright", "React Testing Library"],
  "Payment & Compliance": ["PCI-DSS", "Payment processing", "Stripe", "Fraud detection", "KYC/AML", "SOC 2"],
  "Tools": ["Git", "Webpack", "Vite", "Figma", "Jira"]
}
```
Total: aim for **48–62** individual skills across categories (see **§4** rules—not a short sample list)

**If relevant, create domain-specific category:**
- FinTech → "Payment & Compliance"
- Healthcare → "Healthcare Compliance & Standards"
- Security → "Security & Identity"
- Data → "Data Governance & Compliance"

---

### **5. EXPERIENCE** ({{experienceCount}} entries — follow bullet count guidance above)

**STRICT Requirements (must be followed):**
- Generate exactly {{experienceCount}} job entries; one entry per job in the work history—no more, no fewer.
- **Merge behavior:** If **`details.length` === `base_bullets.length`** (and bases exist), your lines **replace** base **one-for-one** (same bullet count). Otherwise the resume shows **`[...base_bullets, ...details]`**—**all** bases stay, then your additions. If there are **no** `base_bullets`, `details` is the full list for that role.
- **JD gaps vs `base_bullets`:** When the JD stresses a skill or responsibility **not** named in that role’s `base_bullets`, you **must** still reflect it if **credible**—by **rewriting** a related base bullet to fold that theme in, or by **appending** a **long** bullet (**rule 1** limits). **Do not** leave a defensible JD pillar **unmentioned** in **experience** just because the base text was silent.
- **Length & voice:** Push **every** bullet toward **maximum** credible length: senior **IC**/**staff** tone, **precise** product/platform/pattern language, **cross-functional** partners (PM, SRE, security, data), **operational** realities (on-call, incidents, capacity, migrations, contracts with upstream/downstream). For **`experience[0]`** and **`experience[1]`**, bias to the **longest** lines the facts support (often **multi-line** in PDF). Avoid thin one-clause blurbs.
- **Bullet substance:** each line should combine **(a)** business/technical context, **(b)** **your** ownership and decisions, **(c)** **stack**/**API**/**data**/**infra** specifics, **(d)** **scope**/**constraints**, **(e)** **qualitative** or **already-sourced** outcomes (rule **5**). Weave **2–5** JD keywords where credible—**never** keyword stuffing that reads false.
- **JD density:** For **work history #1** (most recent), maximize JD alignment **without** breaking **§0a** credibility or date-appropriate tech below.
- For each work experience, ensure all technologies listed were widely available and commonly used in production during that role's time period; do not include tools, frameworks, or practices released after the role ended or unlikely to have been adopted in that era. Do not force unrelated JD jargon into older roles if it would read false.

**Example Bullet (length + density reference only; strip or replace hard metrics unless they already exist in `base_bullets`—substitute qualitative outcomes instead):**
"Architected and operated a secure payment processing platform on PCI-DSS compliant infrastructure using Node.js 20, PostgreSQL, and Redis clusters, partnering with risk and compliance to implement fraud detection, tokenization, and circuit breakers that processed $500M+ annually while reducing chargebacks by 40%, hardening observability and incident playbooks, and maintaining 99.99% uptime for 2M+ monthly active users across multiple regions."


Return ONLY valid JSON: {"title":"...","summary":"...","skills":{"Category":["Skill1","Skill2"]},"experience":[{"title":"...","details":["bullet1","bullet2",...]}]}
**Final checklist:** Rules **1–7** at the top of this file + **§3**/**§4**/**§5**. **Never** remove `base_skills`. **Never** reduce `base_bullets` count without **same-count** rewrite.
